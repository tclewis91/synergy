<%= form_for(@employee_course) do |f| %>
  <% if @employee_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_course.errors.count, "error") %> prohibited this employee_course from being saved:</h2>

      <ul>
      <% @employee_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
